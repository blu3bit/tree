Project contains: treep (protocol), treehut (app) and simshim (development environment).
Project is written in vanilla JS.
To reduce attack surtface, it use as few third-party dependencies as possible.

